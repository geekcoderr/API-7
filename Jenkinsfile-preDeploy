pipeline {
    agent any
    stages {
        stage('Example') {
            steps {
                script{

                env.DORA=scm.getUserRemoteConfigs()[0].url
                sh("echo ${DORA}")
                }
            }
        }
    }
}