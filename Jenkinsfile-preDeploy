pipeline {
    agent any
    stages {
        stage('Example') {
            steps {
                script{

                env.DORA=scm.getBuildChooser()
                sh("echo ${DORA}")
                }
            }
        }
    }
}