pipeline {
    agent any
    parameters {
        string(name: 'PERSON', defaultValue: '', description: '')
    }
    stages {
        stage('Example') {
            steps {
               sh('echo ${env.GIT_COMMIT}')
            }
        }
    }
}