pipeline {
    agent any
    parameters {
        string(name: 'PERSON', defaultValue: '', description: '')
    }
    stages {
        stage('Example') {
            steps {
                script{

               sh(returnStdout: true, script: 'git log -1 --oneline')
                }
            }
        }
    }
}