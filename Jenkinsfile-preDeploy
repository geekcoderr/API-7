pipeline {
    agent any
    stages {
        stage('Example') {
            steps {
                script{

                env.DORA=scm.getUserRemoteConfigs().tokenize()
                sh("echo ${DORA}")
                }
            }
        }
    }
}