pipeline {
    agent any
    parameters {
        string(name: 'PERSON', defaultValue: '', description: '')
    }
    stages {
        stage('Example') {
            steps {
                script{

                env.DORA=sh(returnStdout: true, script: 'git log -1 --oneline')
                sh("echo ${DORA}")
                }
            }
        }
    }
}