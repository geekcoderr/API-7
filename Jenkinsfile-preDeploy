pipeline {
    agent any
    stages {
        stage('Example') {
            steps {
                script{
                    env.DORA = env.GIT_COMMIT[0..7]
                    env.DORA = scm.getActionDetails()
                    sh("echo ${DORA}")
                }
            }
        }
    }
}