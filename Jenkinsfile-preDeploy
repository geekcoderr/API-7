pipeline {
    agent any
    stages {
        stage('Example') {
            steps {
                script{

                env.DORA=scm.branch
                sh("echo ${DORA}")
                }
            }
        }
    }
}