pipeline {
    agent any
    stages {
        stage('Example') {
            steps {
                script{

                env.DORA=scm.getUserRemoteConfigs()[0]
                sh("echo ${DORA}")
                }
            }
        }
    }
}