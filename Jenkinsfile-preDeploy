pipeline {
    agent any
    stages {
        stage('Example') {
            steps {
                script{
                    env.DORA = env.GIT_COMMIT
                    sh("echo ${DORA}")
                }
            }
        }
    }
}