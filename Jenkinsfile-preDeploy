pipeline {
    agent any
    stages {
        stage('Example') {
            steps {
                script{

                env.DORA=scm.generateCodeBuildCommandList()
                sh("echo ${DORA}")
                }
            }
        }
    }
}