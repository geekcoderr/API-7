pipeline {
    agent any
    stages {
        stage('Example') {
            steps {
                script{

                env.DORA=scm.getUserRemoteConfigs()
                sh("echo ${DORA}")
                }
            }
        }
    }
}