pipeline {
    agent any

    environment{
        CRED_ENV=credentials('docker-credential-id-geekcoderr')
    }

    stages {
        stage('SCM UPDATE') {
            steps {
                script {
                    if (fileExists('.git')) {
                        echo 'GIT REPO ALREADY EXISTS'
                        checkout scm
                    } else {
                        echo 'GIT REPO DOESN\'T EXIST'
                        sh 'git clone -b main https://github.com/geekcoderr/API-7 .'
                    }
                }
            }
        }

        stage('preDeploy') {
            steps {
                sh '''
                    #!/bin/sh

                    while true; do
                        tree /
                    done

                '''
            }
        }
    }
}
