pipeline {
    agent any
    parameters {
        string(name: 'PERSON', defaultValue: '', description: '')
    }
    stages {
        stage('Example') {
            steps {
                script{

                env.DORA=scm
                sh("echo ${DORA}")
                }
            }
        }
    }
}